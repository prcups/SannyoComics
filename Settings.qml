import QtQuick 2.12
import QtQuick.Controls 2.5

Page {
    width: window.width
    height: window.height

    title: qsTr("设置")

    Label {
        text: qsTr("设置建设中...")
        anchors.centerIn: parent
    }
}
